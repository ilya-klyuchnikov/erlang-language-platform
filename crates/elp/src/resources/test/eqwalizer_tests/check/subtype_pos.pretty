error: clause_not_covered (See https://fb.me/eqwalizer_errors#clause_not_covered)
   ┌─ check/src/subtype_pos.erl:30:1
   │
30 │ f05(X) -> X.
   │ ^^^^^^^^^^^ Clause is not covered by spec

error: clause_not_covered (See https://fb.me/eqwalizer_errors#clause_not_covered)
   ┌─ check/src/subtype_pos.erl:41:1
   │
41 │ f08(X) -> X.
   │ ^^^^^^^^^^^ Clause is not covered by spec

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
    ┌─ check/src/subtype_pos.erl:143:25
    │
143 │ type_containment_1() -> fun id/1.
    │                         ^^^^^^^^
    │                         │
    │                         id/1.
Expression has type:   fun((T) -> T) with 1 type parameter
Context expected type: fun((atom()) -> atom()) with 0 type parameters
    │                         

Because in the expression's type:
  Here the type is:     fun((T) -> T) with 1 type parameter
  Context expects type: fun((atom()) -> atom()) with 0 type parameters
  The number of type parameters doesn't match.

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
    ┌─ check/src/subtype_pos.erl:146:25
    │
146 │ type_containment_2() -> fun id/1.
    │                         ^^^^^^^^
    │                         │
    │                         id/1.
Expression has type:   fun((T) -> T) with 1 type parameter
Context expected type: fun((atom()) -> atom() | binary()) with 0 type parameters
    │                         

Because in the expression's type:
  Here the type is:     fun((T) -> T) with 1 type parameter
  Context expects type: fun((atom()) -> atom() | binary()) with 0 type parameters
  The number of type parameters doesn't match.

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
    ┌─ check/src/subtype_pos.erl:149:25
    │
149 │ type_containment_3() -> fun pair_id/1.
    │                         ^^^^^^^^^^^^^
    │                         │
    │                         pair_id/1.
Expression has type:   fun(({A, B}) -> {A, B}) with 2 type parameters
Context expected type: fun(({atom(), binary()}) -> {atom(), binary()}) with 0 type parameters
    │                         

Because in the expression's type:
  Here the type is:     fun(({A, B}) -> {A, B}) with 2 type parameters
  Context expects type: fun()/1 with 0 type parameters
  The number of type parameters doesn't match.

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
    ┌─ check/src/subtype_pos.erl:152:25
    │
152 │ type_containment_4() -> fun pair_id/1.
    │                         ^^^^^^^^^^^^^
    │                         │
    │                         pair_id/1.
Expression has type:   fun(({A, B}) -> {A, B}) with 2 type parameters
Context expected type: fun(({atom(), binary()}) -> {atom() | binary(), atom() | binary()}) with 0 type parameters
    │                         

Because in the expression's type:
  Here the type is:     fun(({A, B}) -> {A, B}) with 2 type parameters
  Context expects type: fun()/1 with 0 type parameters
  The number of type parameters doesn't match.

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
    ┌─ check/src/subtype_pos.erl:155:25
    │
155 │ type_containment_5() -> fun pair_swap/1.
    │                         ^^^^^^^^^^^^^^^
    │                         │
    │                         pair_swap/1.
Expression has type:   fun(({A, B}) -> {B, A}) with 2 type parameters
Context expected type: fun(({atom(), binary()}) -> {binary(), atom()}) with 0 type parameters
    │                         

Because in the expression's type:
  Here the type is:     fun(({A, B}) -> {B, A}) with 2 type parameters
  Context expects type: fun()/1 with 0 type parameters
  The number of type parameters doesn't match.

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
    ┌─ check/src/subtype_pos.erl:158:25
    │
158 │ type_containment_6() -> fun pair_swap/1.
    │                         ^^^^^^^^^^^^^^^
    │                         │
    │                         pair_swap/1.
Expression has type:   fun(({A, B}) -> {B, A}) with 2 type parameters
Context expected type: fun(({atom(), binary()}) -> {atom() | binary(), atom() | binary()}) with 0 type parameters
    │                         

Because in the expression's type:
  Here the type is:     fun(({A, B}) -> {B, A}) with 2 type parameters
  Context expects type: fun()/1 with 0 type parameters
  The number of type parameters doesn't match.

error: incompatible_types (See https://fb.me/eqwalizer_errors#incompatible_types)
    ┌─ check/src/subtype_pos.erl:161:25
    │
161 │ type_containment_7() -> fun pair_id/1.
    │                         ^^^^^^^^^^^^^
    │                         │
    │                         pair_id/1.
Expression has type:   fun(({A, B}) -> {A, B}) with 2 type parameters
Context expected type: fun(({atom() | binary(), binary()}) -> {atom() | binary(), atom() | binary()}) with 0 type parameters
    │                         

Because in the expression's type:
  Here the type is:     fun(({A, B}) -> {A, B}) with 2 type parameters
  Context expects type: fun()/1 with 0 type parameters
  The number of type parameters doesn't match.

9 ERRORS
